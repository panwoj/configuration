server:
  port: 9991
spring:
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.SQLServerDialect
    database: sql_server
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    username: transfer_user
    password: pass
    url: jdbc:sqlserver://localhost:1433;databaseName=Transfers
accounts:
  allow-get-accounts: true